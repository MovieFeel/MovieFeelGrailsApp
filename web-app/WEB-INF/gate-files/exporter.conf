{Token.index}, {Token.class}, "{Token.string}", "{Token.category}"


Everything after the first line in this configuration file is treated as a 
paragraph.

Using this configuration file with a instance type of Token will generate an
output file containing one line for each Token annotation in the input
document. 